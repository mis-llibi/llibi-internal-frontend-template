# Set demo mode to true to use the built-in user without a backend.
NEXT_PUBLIC_DEMO_MODE=false

# Backend URL used during local development.
NEXT_PUBLIC_BACKEND_API=http://localhost:8000

# Backend URL used when NODE_ENV is production.
NEXT_PUBLIC_DEPLOYED_BACKEND_API=https://your-backend-domain.example.com
